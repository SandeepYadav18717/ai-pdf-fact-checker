function PDF_READER() {
    [pdf_upload,setpdf_uploads] = useState(null);

    const handle_file_upload = (event) => {
        setpdf_uploads(event.target.files[0]);
        if pdf
    }
    {
    return (
        <div>
            <input 
                type="file"
                id="file_reader"
            />
        </div>
    );
}
}
export default PDF_READER;